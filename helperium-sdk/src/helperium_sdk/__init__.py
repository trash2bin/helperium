"""Helperium SDK — shared library for all helperium services.

Contains:
- db.* — database abstraction (SQLite / PostgreSQL)
- rag.* — RAG HTTP client and public models
- api.* — HTTP DTO models (canonical source for OpenAPI generation)
"""
